# Copy to .env.local (gitignored) and set your self-hosted server's API base URL.
# This is baked into the APK at build time by scripts/build-apk.sh — the app talks
# only to this host. Example for a LAN/Tailscale server:
EXPO_PUBLIC_API_URL=https://money.example.com/api/v1
