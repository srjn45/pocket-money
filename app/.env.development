# Loaded by `npx expo start` (NODE_ENV=development).
# Points the app at a locally running backend (go run ./cmd/server on :8080).
# Personal overrides go in .env.local (gitignored), which takes precedence.
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
