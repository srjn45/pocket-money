# Loaded by production bundling (`expo export`, Gradle release builds, CI).
# This is the canonical production server URL baked into released APKs.
# CI's EXPO_PUBLIC_API_URL repo variable, if set, overrides this file;
# locally, .env.local (gitignored) overrides it too.
EXPO_PUBLIC_API_URL=https://pocket-money-srjn45.fly.dev/api/v1
